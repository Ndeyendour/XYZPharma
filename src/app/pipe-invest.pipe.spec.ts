import { PipeInvestPipe } from './pipe-invest.pipe';

describe('PipeInvestPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeInvestPipe();
    expect(pipe).toBeTruthy();
  });
});
